@using Blog.Core.Models.Contexts
@model Blog.Core.Models.Contexts.PageContext 

@foreach (var post in Model.Paginator.Posts)
{
    <div class="blog-post-list-item">
        @Html.Raw(post.Content)
    </div>
}
